import test from 'node:test';import assert from 'node:assert/strict';import {newRun} from './engine.js';import {relicPreview,relicRequirements,relicPreviewMarkup} from './relic-preview.js';import {grantRelic} from './relics.js';
test('relic previews preserve player and compare live movement and attack stats',()=>{const p=newRun(1).player,original=JSON.stringify(p);assert.ok(relicPreview(p,'boots').rows.some(r=>r.label==='이동속도'&&r.after>r.before));assert.ok(relicPreview(p,'quiver').rows.some(r=>r.label==='초당 사격'&&r.after>r.before));assert.equal(JSON.stringify(p),original);});
test('inactive effects describe prerequisite and ultimate cooldown uses selected branch',()=>{const p=newRun(1).player;assert.ok(relicRequirements(p,'iron').length);assert.ok(relicRequirements(p,'sun').length);assert.ok(relicRequirements(p,'halo').length);p.ultimate=1;p.evolutions={ultimate:'turret'};const row=relicPreview(p,'rain').rows.find(r=>r.label==='궁극기 재사용(초)');assert.equal(row.before,20);assert.equal(row.after,16);});
test('exchange preview removes sacrificed relic including heart loss without changing the build',()=>{const p=newRun(1).player;grantRelic(p,'heart');const before=JSON.stringify(p);const rows=relicPreview(p,'windSeal','heart').rows;assert.ok(rows.some(r=>r.label==='최대 체력'&&r.before-r.after===1));assert.equal(JSON.stringify(p),before);const html=relicPreviewMarkup(p,'windSeal','heart');assert.ok(html.includes('잃는 효과'));assert.ok(html.includes('20처치'));});
