import test from 'node:test';import assert from 'node:assert/strict';import {bindCombatAction} from './touch-actions.js';import {monsterPotionReward} from './survival.js';import {newRun} from './engine.js';
test('secondary touch activates immediately once; keyboard and mouse clicks still work',()=>{const button=new EventTarget();let actions=0;bindCombatAction(button,()=>actions++);const send=(type,props)=>{const e=new Event(type,{cancelable:true});Object.assign(e,props);button.dispatchEvent(e);};send('pointerdown',{pointerType:'touch',isPrimary:false,pointerId:2});assert.equal(actions,1);send('click',{detail:1});assert.equal(actions,1);send('click',{detail:0});assert.equal(actions,2);button.disabled=true;send('pointerdown',{pointerType:'touch',isPrimary:false});assert.equal(actions,2);});
test('monster potion rewards convert only at five potions and only on a successful roll',()=>{const s=newRun(1),room={};s.player.potions=4;assert.equal(monsterPotionReward(s,room,{x:100,y:120},()=>0),'potion');assert.equal(s.player.potions,5);assert.equal(monsterPotionReward(s,room,{x:100,y:120},()=>.9),null);assert.equal(monsterPotionReward(s,room,{x:100,y:120},()=>0),'essence');assert.equal(s.player.potions,5);assert.deepEqual(room.essences,[{id:'attack',x:100,y:120}]);assert.equal(room.battlePotions,1);});
